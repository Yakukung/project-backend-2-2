export interface SelectedPost {
    post_id: number;
    user_id: number;
    score: number; // Assuming 'score' is a numeric value
    time: string;
    picture: string;
    eloRating: number; // Assuming 'eloRating' is a numeric value
}
